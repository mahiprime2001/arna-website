# Arna website — static Vue site served by nginx inside the container,
# exposed on host port 4300. Your existing host nginx sits in front of it and
# serves https://arna.ifleon.com (see nginx/arna.ifleon.com.conf).
#
#   docker compose up -d --build
#
# Change "4300" below if that port is already taken on the host.

services:
  website:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:4300:80"
